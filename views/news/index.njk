---
layout: hero-image
title: Artikels
tags:
subtitle: Hier komen alle artikels!
eleventyNavigation:
    key: News
    order: 2
---
<main class="news">
<h1> {{ title }}</h1>
<ul>
    {% for article in news %}
        <li class="news--list"> 
            <div class="news--list--content">
                <h2 class="subtitle">Post by: {{ article.source.name }}</h2>
                <h3>{{ article.title }}</h3>
                <h3>{{ article.description }}</h3>
                <img src={{ article.urlToImage }} alt="">
                <p>{{ article.content }}</p>
                <p> <strong>{{ article.author }}</strong> • {{ article.publishedAt }} </p>
            </div>
        </li>
    {% endfor %}
</ul>
</main>